# interval-manager
Manage intervals: add, remove intervals

## Requirements
 - Java 21

## build and test
```shell
./mvnw clean test
```

## Discussion points
- Using `LinkedList` instead of `ArrayList` to avoid extra memory?